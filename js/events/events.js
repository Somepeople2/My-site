function age() {
    let age = Number(prompt('How old are you?'))
    alert(`in 10 years you are going to be ${age} years old.`)
}

function sheep() {
    let count = Number(prompt('How many sheep are there'))
    for (let i = 1; i<=count; i++) {
        alert(`${i} sheep`)
    }
}